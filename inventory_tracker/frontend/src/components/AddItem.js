import React, { Component } from 'react';

export default class AddItem extends Component {
    constructor(props) {
        // always call super constructor
        super(props);
    }

    render() {
        return <p>This is page to add an item</p>;
    }
}